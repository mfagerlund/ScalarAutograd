import type { DemoMetadata } from '../types';

export const metadata: DemoMetadata = {
  id: 'circle-formation',
  title: 'Circle Formation',
  description: 'Watch points connected by lines smoothly align into a circle using gradient descent optimization. Compare SGD, Adam, and AdamW optimizers.',
  difficulty: 'beginner',
  tags: ['geometry', 'optimization', 'visual'],
};
