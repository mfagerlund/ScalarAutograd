import { CircleFitting } from './CircleFitting';
import { metadata } from './metadata';
import type { Demo } from '../types';

export const circleFittingDemo: Demo = {
  metadata,
  Component: CircleFitting,
};
