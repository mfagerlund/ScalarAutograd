import { SelfAvoidingRope } from './SelfAvoidingRope';
import { metadata } from './metadata';
import type { Demo } from '../types';

export const selfAvoidingRopeDemo: Demo = {
  metadata,
  Component: SelfAvoidingRope,
};
